program Project1;

uses
  Forms,
  Unit1 in 'Unit1.pas' {Main},
  Unit3 in 'Unit3.pas' {Oders},
  Unit4 in 'Unit4.pas' {MenuFm};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TMain, Main);
  Application.CreateForm(TOders, Oders);
  Application.CreateForm(TMenuFm, MenuFm);
  Application.Run;
end.
